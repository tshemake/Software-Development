using System;

/*
 * Now let's see how clear the previous material was!
 */

namespace step_11
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Function function Person (name, age, year) {this.name = name;} will create a constructor for the person");
            Console.WriteLine("The this keyword is used to access the object from which it is called");
            Console.WriteLine("If you assign any code to a property, then we get a method");
        }
    }
}
